services:
  cloudflared:
    container_name: cloudflared
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    ports:
      - 60123:60123
    command: tunnel --no-autoupdate --metrics 0.0.0.0:60123 run --token ${CLOUDFLARE_TOKEN?}
